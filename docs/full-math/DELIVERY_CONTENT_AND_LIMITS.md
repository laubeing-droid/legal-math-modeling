# 包内是什么，执行者还必须实际完成什么

## 本次已经提供

完整原计划、公式与证明步骤、57目标实施卡、134业务数学实例卡、9项EXT、14法律族、7个通用根及统一C07出口、原参考算法和补充算法、Lean参数化候选、逐项执行作业、正常/反例测试、同次编译与全量完成规则、GitHub单一权威工作流修改器、数学完成后导出合同。

## 不是本次已经取得的结果

`FullMath.Contracts`和`FullMath.Acceptance`的217项最终实例、受支持范围的全部通用根证明以及对应GitHub完成记录，必须由施工Agent依据这些资料连续实现并运行。包中没有虚构其源码或通过记录。`BINDINGS.json`初始为空，故全量门现在应当报告INCOMPLETE；没有把这个门的自测通过包装成完整数学通过。

这个交付改变的是完整工作的范围、实施材料和停止条件，不是提前授予“数学已完成”证书。原固定示例只复用、不返工；也不再是总交付终点。算法参考可以改进和替换，但必须重新证明同一独立合同。

## 不可互相替代

包验证检查字节；Python测试检查特定输入；Lean证明检查命题及其依赖；语义核对检查该命题是否表达既定问题；真实资料核验和预测验证检查外部前提；生产集成检查实际调用。不得用前一项冒签后一项。

所有数学目标统一要求本轮执行到底。真实数据、现实法源和生产接入缺项仍留名、留原因、留接口，不能用它们阻止可执行数学任务，也不能用合成数据或空假设把它们洗成完成。
