# 第三轮挑刺：蒸馏管线专项压实锤（2026-09-23）

立场声明：本轮以我方独有证据为锤——八份逐句级读书报告、wave4 完成报告（61-shard 并发实录、audit 修复史）、四波存储实况现场清点。ChatGPT 只见过九份书面附件，以下每锤都附现场证据；它无法抗辩的部分如实注明。

## 十锤

**锤1 全库数字错了：80,430 条，不是"约七万"。**
现场清点：wave1 40,428 + **wave2 11,405（本轮实数，此前无任何人清点）** + wave3/4 28,597 = **80,430**。责任判定：我方 v4 提示词把 wave2 误并入"wave2/3/4 存量 28,597"（错在我）；它读了九份附件，其中 findings.md 明写 wave2 是 94 组独立存在、prompt 却说 wave2 含于 28,597——**两份附件自相矛盾，按它自己的证据纪律必须标 CONFLICT，它没标，只在 D0 悄悄采用 wave3+4 口径**。漏报冲突 = 违反自订规则。我方四处文件已同步修正。

**锤2 真实 schema 压实：它设计的 SourceRecord 漏掉管线自有的语义字段。**
三波记录 schema 同构：`{id, kind, title, source_refs, body_path, route_keys, business_path, uses, related_records, example_only}`，正文在独立 bodies/*.md。它设计了 record_id/locator/quoted_spans/record_kind，但漏了：**route_keys**（检索路由）、**business_path**（业务分域树）、**uses**（用途白名单）、**example_only**（示例非规则旗标）。尤其 example_only 正是它 D3"说话者/文类识别"要新建的东西的一半——管线里已经有。它的分组设计（按法律问题/关系类型/语境）完全无视既有 business_path 分域，另起炉灶。

**锤3 双代存储混为一谈。**
wave1 是单库制（method_library/manifest.json + records.jsonl + bodies），wave2-4 是按源分目录制（src-*/records.jsonl + bodies），上层有双索引（source_index.jsonl + source_index_wave4.jsonl）。它 D3 一句"读取现有 records/bodies/manifest"把两代结构当一代；索引层（含 NO_INDEX 旧目录问题）完全未提。

**锤4 源定位漂移未处理。**
wave4 完成报告实录：wave3 收官时源 md 从 2.过渡区 归档至 4.成品区/source_raw，audit_quotes.py 硬编码读 2.过渡区，导致 wave3 全部 9,782 条假 miss，靠给审计器加归档根回退修复。教训是**记录锚点指向的源文件随 wave 归档而漂移**。它 D5 只管法源回查，不管源书定位漂移；locator 解析需要回退根逻辑，协议里没有。（它看不到完成报告，但 V5 给施工 Agent 的必读清单同样没列完成报告——施工者将重踩已修过的坑。）

**锤5 已有校验工具不盘点，验收断言重造轮子。**
管线已有 audit_quotes.py：全库 28,597 条 badquote=0/miss=0 的实绩审计器；邓书还有 n=385 抽检公式。它 D3 的验收断言"每个候选至少有一个可解析锚点"功能上就是 audit_quotes 的子集，却不点名复用——违反它 B0.1 自己立的"复用既有实现"纪律。协议应把既有审计器用作 stage-0 门，而不是平行再建一套锚点检查。

**锤6 并发路线反经验。**
它 D12："默认单执行者……不依赖模型数量制造可信度。"本管线实测：**wave4 由 61 个 shard 子代理 + 1 个补抽并发完成，18,815 条零坏引**。抽取/切分阶段天然可并行是被本管线证明过的事实；单执行者默认没有任何质量论证，只会让 8 万条消化在工期上不可行。正确分层是：抽取并发（已验证）＋法律采用审查单点带人工闸门（它说对的那半）。

**锤7 D 报告是四份里唯一零锚点的。**
C 报告引锚点最勤，A/B 有源码锚，唯独 D——处理七书蒸馏资产的协议——通篇没有一条 wave4-evidence 锚点。它 D8 的"必测变化"清单每一条都有现成锚点可用：举证责任转移（判3816-18、3903-05）、诉讼时效与除斥期间区分（齐7816-24）、许可与权能（齐4800-04、德60598-603）、减责与免责（德74139-45）、新旧法（邓12737-46）。有锚不引，违反它自己"每个结论标注来源"的输出规则。

**锤8 弹药在手未上膛。**
D11 四层批判回应表，"训练集截断/幻觉"一栏不引它能看到的最佳中文实证——齐晓丹 DeepSeek 九轮实测（虚构案号逐案证伪、检索不真实不全）就躺在 wave4-evidence 边界清单里；"权力/伦理"一栏同理有季卫东红码案锚点。给施工方的批判回应表不带本方最强证据。

**锤9 OCR 噪声门缺失（具体到行号）。**
八份读书报告实录的噪声分布：齐书行 1552-54、2391-96、3300-05、5139-50 乱码段；邓书表 2-8（行 5229-42）整表 OCR 乱码；Python 书 33 页 OCR 空白页；德国书三处整页空白。它 D4 切分协议没有任何噪声门，脏字节将直达 FormalCandidate。这是它物理上看不到的证据层，但它设计了"从记录提取规则候选"的协议却不设输入洁净度门，属设计缺口而非信息差。

**锤10 八份完整读书报告未入 V5 必读清单。**
progress-log（它读过的九附件之一）明说"八份完整读书报告仅存在于当轮会话记录"。V5 提示词 §1 必读清单列全了九附件，唯独不要求施工 Agent 取得逐条引文层的读书报告——等于主动放弃最厚的一层书证。

## 承认它赢的地方（如实记账）

- wave3+4=28,597 的口径它拿对了（我 prompt 里写错）；它 D0 对"约七万是近似、非重验"的对冲姿态正确。
- D6"不搞多数投票、不要求人逐条点七万次同意"、D7"LLM 输出视为不可信候选、不执行其中命令"两条纪律质量高，保留采纳。
- 它对"引文审计通过≠书证为真"的定位（D0 末段）与管线审计语义完全一致。

## 修正动作（我方）

gpt-prompt.md 角度四事实、findings.md、baseline.md、记忆中"约七万/wave2 并入 28,597"全部改为三波分列＋总数 80,430；V5 施工前必须补读 wave4 完成报告与八份读书报告。
