# 02 逆向工程审计：最终校准版

日期：2026-06-11

## 1. 审计判定

2026-06-08 的数学模型草稿有工程价值，但存在过强证明叙事。经过 Codex 形式化验证、Kimi 严格证明返工和法律数据验证后，应采用“证据分级”而不是“一揽子证明”。

## 2. 已修正的高风险命题

| 风险命题 | 审计结果 | 处理 |
|---|---|---|
| 完整 evaluator 单调 | 反例推翻 | 拆成 Horn + AAF |
| 18 个定理由 Galois 推出 | 不成立 | 改为架构目录 |
| 跨法域全函子/自然变换不存在 | 真实数据不足 | 改为 obstruction witnesses |
| 全维 Banach pricing | 真实 timesheet 缺失 | 改为 toy scalar submodel |
| 法律 privilege 推出 epsilon | two-model witness 反证 | 改为 policy candidate |
| graph similarity 是 metric | 自反性/同一性反例 | 改名为 score，不作 metric |

## 3. 已修改 theory 文件

| 文件 | 修改目的 |
|---|---|
| `theory\model_status.py` | 新增统一证据账本 |
| `theory\__init__.py` | 改为 evidence-calibrated theory index |
| `theory\__main__.py` | 新增 `python -m theory` 入口 |
| `theory\category_theory_rosetta.py` | 降级为 Rosetta obstruction engineering boundary |
| `theory\banach_pricing_contraction.py` | 降级为 toy scalar contraction + real data insufficiency |
| `theory\dp_legal_privilege.py` | 改为 legal release class + policy epsilon |
| `theory\abstract_interpretation_unified.py` | 改为架构目录，不再声称派生所有定理 |
| `theory\argumentation_horn_unification.py` | 改为 stratified Horn + AAF 方案 |

## 4. 验证结果

执行：

```powershell
$env:PYTHONIOENCODING='utf-8'
$env:PYTHONUTF8='1'
C:\Users\being\AppData\Local\Programs\Python\Python312\python.exe -m compileall -q "...\juris-calculus\theory"
```

结果：通过。仅 `rough_set_discretionary.py` 存在旧 docstring escape warning，不影响本轮修改。

执行：

```powershell
cd "...\juris-calculus"
C:\Users\being\AppData\Local\Programs\Python\Python312\python.exe -m theory --summary
```

结果：通过，输出证据校准后的 7 项核心 claim。

## 5. 审计结论

该目录现在可作为“数学模型与工程设计准备版”。它不再把早期猜想包装成证明，而是把每个模型命题映射到：

1. 证据状态；
2. 禁用声称；
3. 工程动作；
4. 后续代码提升目标。

