# 全任务执行表

关闭依赖不等于开始施工前置条件。ROOT为当前近端主线；BRFIX已存在部分代码不得重写；EXT01/02复用。

## T00 冻结主体、纠正Rosetta宣称

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：无

施工：

记录实际LMM、JC、规则/数据版本，不继承旧commit结果。

应用补丁改名sample_mapping_not_total，保留原公式；清查Lean审计/论文/文档中的对应引用和残留过度解释。

验收：

原命题保持、无泛函子不可能性宣称、GitHub CI重新通过。

## T01 建立独立全局见证和语义

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T00

施工：

固定Φ与Solutions，不由程序输出定义。

将确切解、部分解、外包络三种证书使用同一指称保持定义。

验收：

给出具体参数、法律/模型/效用语义和每个接口关系。

## T02 类型索引、序列化和当前主体绑定

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T01

施工：

增加争点/模型/目标/语义范围元数据，兼容现有公共入口，Schema由代码生成。

结构键往返与拒绝跨案/跨版本/跨目标组合。

验收：

数据来回一致；分隔符、摘要错绑、模型版本错绑反例拒绝。

## T03 授权、事实类别、假设与排除传播

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T02

施工：

复用现有信任链和P0/P1。

提供条件情景入口，不能给假设伪造verified_fact。

排除证据失效其下游概率特征与金额/策略。

适配当前JC免密钥本地运行；LocalRecord只表示本地记录。机构权威来自既有宿主已核验来源，不新增签名/激活系统。

验收：

所有构造器准入测试；排除/撤回后的依赖重算；律师分析不冒充法院行为。

## T04 Horn编译与真实增量精化

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T03

施工：

复用已证明有限闭包。

证明工作队列不变量和编译映射；非add-only全量回退。

验收：

对当前实现证明所需关系或明确交叉核验级别；不得假设增量正确再宣布已证。

## T05 论证树生成、有限深度与完整载体

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T04

施工：

按独立语法定义高度d，证明健全/完备。

结构ID保留规则、叶、子树和假设；不启用未证循环商化。

验收：

同结论不同支持不合并；无根循环不产出；高度范围可核对。

## T06 攻击/优先政策和扩展成员反射

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T05

施工：

按子论证编译攻击并证明双向覆盖。

分别完成grounded/complete/preferred/stable检查器反射。

验收：

方向、子论证攻击、优先政策遗漏、prefix-maximal等反例永久回归。

## T07 完整/部分枚举和查询

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T06

施工：

把A/R/U扫描不变量实例化到扩展语义。

完整性由范围覆盖＋无未决推出；区分NoExtension与空的found。

验收：

两条总契约在声明范围内成立；不完整查询不被展示成全称支持。

## T08 精确金额/期限/单位/舍入

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T03

施工：

按有理数/整数计量，法定口径为显式输入。

补转换和舍入证明，保持来源及依赖。

验收：

跨币种/跨债务/日月混用拒绝，过付保留，计息区间分段正确。

## T09 合法可行域与离散政策

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T07, T08

施工：

区分合法性与客户效用，保留原可行集合。

通过独立法律清单映射硬约束、例外及待判断事项。

验收：

部分发现跨度不能冒充总区间；不连通可行域不被插值填满。

## T10 LP/凸性/MILP证明证书

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T09

施工：

固定max/min不等号约定并证明变换。

精确PSD/凸语法校验；有界整数穷举参考；B&B节点覆盖和下界证书。

验收：

错误对偶、假PSD、未关闭整数搜索节点不能得到optimal/complete。

## T11 Banach指定子模型落地

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T10

施工：

证明实数闭区间投影非扩张与参数k<1。

实例化存在/唯一/收敛/残差并连到该二次目标最优性。

验收：

不满足算子结构或0<ηa<2时不走Banach；不声称一般法律评价收敛。

## T12 有限BN与精确变量消去

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T03, T07

施工：

校验状态/CPT/DAG/证据身份，证明联合归一化与后验。

证明VE因子不变量；纳入证据排除和同源依赖。

验收：

全枚举与VE对照；零质量不兼容；不重复计入复印证据。

## T13 参数学习、竞争模型和误设

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T12

施工：

证明Dirichlet/Beta比值与上升阶乘。

明示模型先验/似然来源，计算真实数据后验；保留未建模解释和后验污染界。

验收：

改数据改后验；条件化前后ε不可串用；搜索剩余质量有界。

## T14 胜率目标、可信区间和校准算法

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T13

施工：

声明party/issue/remedy/stage/cutoff/horizon/event，按目标分模型。

真实训练/独立校准/独立测试，原始可信区间与校准点预测区分。

验收：

重复纠纷与事后特征泄漏拒绝；测试标签不改模型；概率不改变规范结果。

## T15 真实数据的外部验证

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T14

施工：

从真实有权使用来源构建预测时点材料和结果标签；记录公布选择与缺失。

预注册开发范围/阈值和留出测试，报告分层、时间稳定性及适用边界。

验收：

真实数据才可发对应经验标签；未执行不影响参考数学编译，但不可称已校准胜率。

## T16 共同证明责任/推定/程序引擎

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T03, T07

施工：

分离production、persuasion、标准、阶段、触发、反驳、后果。

未知事实与未完成评估分开，条件意见与制度裁判分开。

验收：

跨争点/阶段/标准错绑拒绝；事实未证只产生该规则声明的法律后果。

## T17 民商及专门民事规则族全面展开

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T16

施工：

按合同/物权/侵权/婚姻继承/劳动/公司/IP/金融/消费者/破产/海事建立完整争点slot清单。

每个领域按产生、消灭、抗辩、免证、特殊证明、例外、阶段逐条审核，不能止步37模板。

验收：

对声明领域snapshot中的全部requiredSlots有实现/不适用见证/具名未决，非自定规则数覆盖率。

法律解释审定不能由相同LLM生成并独自评分，保留人工独立基准。

## T18 刑事全链与量刑规则族

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T16, T09

施工：

公诉/自诉/证据合法性/构成阶层/责任/量刑/特别程序分开。

接真实法定刑和量刑指导/地方细则全文，不捏造百分比或统一证明阈值。

验收：

排除后重算；软件超时不判无罪；裁量政策、单位、例外可复核。

## T19 行政、复议、赔偿与专门程序

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T16, T09

施工：

拆行为合法性/履责申请/损害/例外，不能把所有事实都分给被告。

接2026起诉期限、复议实施条例、仲裁法、国家赔偿及执行/上诉/再审规则。

验收：

实体与程序结果区分；机构、证明标准、时间范围不串用。

## T20 法源变化与语义覆盖全集

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T17, T18, T19, T04

施工：

从外部审定法源/教义清单确定requiredSlots，而非从已有代码定义所需范围。

自动法源变化定位受影响规则并触发重新解释/验证，保留未依赖变化的结果。

环境规则族增加L20、L21：按2026-08-15前后法律事实和特别衔接条款选法，复核新证据司法解释；不得只按审判日期一刀切。

验收：

2025–2026法律切换用具体日期/过渡规范；已废止法不当默认；不得宣称全部中国法已形式化。

## T21 补证、和解、履约与有限机制

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T10, T13, T20

施工：

效用与合法行动集接同一语境；信息价值有假设、净成本及证据合法性。

分别核验DSIC/BIC/IR/预算/ε-regret，合法性不被效用覆盖。

验收：

部分偏离枚举不得报全局regret界；司法可判集合与谈判合法集合区分。

## T22 跨层组合证明与具体联合实例

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T11, T14, T20, T21

施工：

实例化每个本次实际算法的关系反射与外包保证，保留共享参数。

把全领域条件后果接概率/金额/策略；给出同一请求的真实联合见证。

验收：

不仅import所有定理；根证明实际使用各实质接口；存在未决分支不能全局complete。

## T23 JC正式入口与独立checker接入

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T22, T02

施工：

沿用唯一Application，不把参考demo当第二生产内核。

实现真实input/trace/spec反射和独立证书checker，三入口同语义。

私有商业/经验数据只通过既有插件/宿主边界使用，公共内核不内置客户资产。

目标JC更新为8dac071bda42c4db609738a7505b6893c32afbe1；先跑现有keyless公共入口smoke，再将V2.1能力真正接入ApplicationV4；不得把smoke当新能力精化。

验收：

测试必须实际调用安装后的JC公共入口而不是返回固定fixture；CLI/MCP真实传输及错输入矩阵。

## T24 统一CI、证书、声明最终对齐

状态：PENDING_TASK_SPECIFIC_CLOSURE

依赖：T23, T00

施工：

先运行参考工作流；各完整证明完成后并回原正式CI，新增目标进root和全部公理审计。

绑定实际主体/版本/证据；按数学/法律解释/经验三个字段批准相应声明；真实经验声明另依赖T15。

禁止无检查平铺工件造成同名覆盖，不将skip/timeout当成功。

数学参考门不等待T15；只有externally_validated_forecast声明必须验证T15/E01。同一subject分别记录各轴结果。

验收：

完整解等式/部分子集/全称外包与当前运行对应；真实经验标签只在T15实测通过时允许。

## REV01 求解对象、符号表示与统一指称

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：无

## REV02 共同主体、非丢失编码与结果绑定

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01

## REV03 检查器来自可信语义注册而非自报函数

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01, REV02

## REV04 金额/概率/成本/合法策略的实质根桥接

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01, REV02, REV03, REV05, REV06, REV07

## REV05 复用既有授权链和全领域责任语义

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV02

## REV06 新旧论证关系、深度范围与扩展保持

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01, REV02

## REV07 模型数值来源与材料敏感胜率能力

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01, REV02

## REV08 保留已有数值防护并闭合实际计算证明

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01, REV02

## REV09 条件证据依赖、CI范围和声明绑定

状态：REFERENCE_REVISION_DELIVERED_PROOF_AND_RUNTIME_PENDING

依赖：REV01, REV02

## EXT01 复用结果表示/checker，扩展注册语义

状态：REUSE_NOT_REOPEN

依赖：REV01, REV03

施工：

投影类型、证书、精确/内/外界分型，不重做已完成修订

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT02 复用主体键，只补新语义作用域

状态：REUSE_NOT_REOPEN

依赖：REV02

施工：

不重开拼接误报；新模型/预测/策略范围需迁移和绑定证明

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT03 profile化结构论证合理性与新旧映射

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：T05, T06, REV06

施工：

按明确严格/可废止规则、子论证、contrary、偏好前提证明；不能静默删扩展改原语义

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT04 投影原像、共同Γ和实际约化/CEGAR

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：EXT01, EXT02

施工：

γΓ=Γ∩⋂π⁻¹γ；保持Γ下双向包含；预算中断保留健全外界；图连通非证明

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT05 来源代数、ATMS及事件概率连接

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：T04, EXT03, T12

施工：

替代支持/撤回/包含极小/全部极小分开；保留重叠事件关系，3/8不能算7/16

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT06 保形秩、数据用途和统计组合

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：T14, EXT04

施工：

边际/条件/筛选后覆盖分开；PAV与保形校准数据不擅自复用；支持时变条件风险扩展

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT07 有限SCM与部分识别优化证书

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：T12, T13, T10, EXT04

施工：

空/未知可行分开；Exact、tight、attained、集合形状分开；非凸允许可靠外界

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT08 有限时域合法策略及稳健共享参数

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：T21, EXT04

施工：

合法行动/对手/权限/期限，非矩形共享θ不逐步换最坏值；Bellman条件满足可正式保证

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## EXT09 三领域和工作产品真实根组合

状态：PLANNED_PROOFS_NOT_CLOSED

依赖：EXT03, EXT04, EXT05, EXT06, EXT07, EXT08, T22, ROOT08

施工：

至少民事、刑事、行政或劳动三条＋交易文件压力场景；合取/导入/有限例子不替全域

验收：

产生具体算法、独立语义、证明、反例与实际证据，不只登记字段。

## BRFIX-01 可运行双文件业务根

状态：REFERENCE_IMPLEMENTED_TESTED

依赖：无

尚缺：根到独立JointSem的Python精化待证

继续关闭：ROOT06

## BRFIX-02 实际JSON回读与内容核验

状态：REFERENCE_IMPLEMENTED_TESTED

依赖：无

尚缺：一般DOCX/PDF/自由文本不在范围

继续关闭：ROOT05

## BRFIX-03 已选完整输入绑定

状态：SYNTHETIC_REFERENCE_IMPLEMENTED_TESTED

依赖：无

尚缺：宿主输入来源、真实数据和模型采用关系待接

继续关闭：ROOT02, ROOT07

## BRFIX-04 原始残差与clipped余额期望桥接

状态：8_LEAN_DRAFTS_CI_NOT_RUN

依赖：无

尚缺：保持原代数定理，证明新对应及应用条件

继续关闭：ROOT04

## BRFIX-05 日志与报告同次生成

状态：EXECUTED_COHERENT_85_TESTS

依赖：无

尚缺：不同Python版本必须实际CI重跑

继续关闭：ROOT08

## BRFIX-06 根规格、解析、checker与业务需求精化

状态：PENDING_FORMAL_INSTANTIATION

依赖：无

尚缺：不能用check布尔合取替代业务语义

继续关闭：ROOT06, ROOT08

## ROOT01 独立有限本金业务语义与需求满足

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：无

施工：

定义Ω(I0)、JointSem、TaskSat_Q、Protected_Q，不引用check布尔值或renderer输出作为定义。

验收：

JointSem中每个情景满足来源/身份/条件及守恒互补；Q固定当前双文件任务，非任意任务万能类型。

## ROOT02 固定原输入与跨语言无损编码

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT01

施工：

从宿主既有MatterStore/CAS任务记录取I0；编码解码与主体迁移一一对应；生产者输出不参与选择expected。

验收：

CheckBinding(I0,I)=true ⇒ I=I0；同版本换参数拒绝；合法新参数开新任务而非全部禁止。

## ROOT03 实际条件编译、情景覆盖与本金检查反射

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT01, ROOT02

施工：

证明后序栈compile/execute等价独立guard语义；笛卡尔积/位掩码与Ω双向覆盖；实际check接受推出逐世界C/U唯一。

验收：

不能只证明递归解释器；覆盖命题不从result反推；超时未决保留。

## ROOT04 非负本金/超付/事件概率及策略实例

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT03

施工：

用C而非R计算Analytics；权重来自I0同一Ω；证明E[C]−E[U]守恒、阈值事件及合法候选集与selected对应。

验收：

P=100,q=300,p=2/5得到E[R]=-20,E[C]=60,E[U]=80；阈值0分别3/5和1。

## ROOT05 实际两文件解析与精化

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT02, ROOT03, ROOT04

施工：

闭合UTF-8文本/严格JSON字节→对象→受保护语义；拒绝额外键、重复键、浮点、NaN、伪null、丢条件；两文件同一主体。

验收：

ReadBoth(d1,d2)=Protected_Q(W,m)；旁路JSON正确不代正文；文件改名/替换/读后变动按快照绑定。

## ROOT06 实例化根定理与具体checker连接

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT01, ROOT02, ROOT03, ROOT04, ROOT05

施工：

由ROOT01–05的真实算法定理取得一个共同W，不以checks的合取替代JointSem；桥接到ULM04/ULM16既有义务。

验收：

WF(I0)∧CheckBundle=accept ⇒ ∃W,Worlds(W)=Ω(I0)∧JointSem∧TaskSat_Q∧ReadBoth=Protected_Q。

## ROOT07 真实JC/Harness输入与交付接线

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT02, ROOT05, ROOT06

施工：

JC唯一公开本地链、宿主保存I0、真实两份字节回读；生产使用唯一ContextKey而非两个可漂移副本。

验收：

真实入口取得同一输入/运行/文件快照；替换整个m和a不能继承旧任务；原宿主已有联调不被称为空白。

## ROOT08 同版本CI与有限根接受

状态：PENDING_CONCRETE_IMPLEMENTATION_OR_PROOF

依赖：ROOT06, ROOT07

施工：

固定Lean/mathlib，真实编译24BR草稿与合并模块；校验具体根声明的类型和依赖、运行精化及文件回读证据。

验收：

只关闭有限合成业务根；一般C07、134项法律实例、法律审核和真实胜率保持各自状态。
