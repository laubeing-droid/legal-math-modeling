"""Specification-side semantic boundary for legal-math-modeling.

The package intentionally avoids eager wildcard re-exports so that individual
modules may be executed directly without import-order side effects.
"""
